[pytest]
asyncio_mode = auto
; 전역 싱글턴 async 엔진(app.db.session)이 커넥션 풀을 이벤트 루프에 묶으므로
; 모든 테스트가 하나의 세션 루프를 공유해야 한다
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = session
testpaths = tests
